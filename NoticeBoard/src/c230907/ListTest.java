package c230907;

public class ListTest {
	//자료구조 => Collection = 자료를 저장하는 방법
	//java에서는 자료구조를 컬렉션으로 구현해 두었다.
	//List는 뭘까?
	//ArrayList => 가변 배열
	//LiunkedList => 노드를 통한  
	//Vector => 수학적인 백터
	//LinkedList
	//node <-> node <-> node double이 이미 구현되어있기 떄문에 single은 구현되어있지 않다.
	//node(Start) -> node -> node(end) -> node(Start) = Circle
	//node를 일자로 연결하는게 아닌 각각의 node에 다른 node를 추가로 붙이는 방식을 트리형식이라 한다. 
	//보통 기준을 정해 각각의 node에 원하는 데이터가 들어가도록 하는 것.
	//BigData => LinkedList
}
